import { chromium } from "playwright"; import { serve } from "./serve.mjs";
const SP=process.env.SP; const server=await serve(8903);
const browser=await chromium.launch({args:["--use-gl=angle","--use-angle=swiftshader","--enable-unsafe-swiftshader"]}); const page=await (await browser.newContext({viewport:{width:1000,height:640}})).newPage();
await page.goto("http://127.0.0.1:8903/?silent"); await page.waitForFunction(()=>window.__dd&&window.__defglb&&window.__defglb.list().totem&&[0,1,2,3].every(i=>window.__defglb.list().totem[i])&&window.__defglb.list().harpoon&&window.__defglb.list().harpoon[0]&&window.__dd.heroModel()&&/v2/.test(window.__dd.heroModel().label),null,{timeout:120000});
const r=await page.evaluate(()=>{ const d=window.__dd; d.start(); d.step(1/60,3); d.S.mana=5000; for(const x of d.defs.slice()){ d.setHero(x.x,x.z+1,0); d.sell(); } const t1=d.place("totem",13,13,0); const t4=d.place("totem",19,13,0); t4.lvl=4; const h=d.place("harpoon",16,12,Math.PI); d.step(1/60,90); d.setHero(t1.x+3,t1.z+9,Math.PI); d.setCam(Math.PI,.42,9); d.step(1/60,40); document.getElementById("hud").style.display="none"; window.__freeze=true; return {t1:[t1.x,t1.z],t4:[t4.x,t4.z],hero:[d.hero.x,d.hero.z],buff:h.buff}; });
console.log(JSON.stringify(r)); await page.waitForTimeout(250); await page.screenshot({path:SP+"/parts/shots/totem.png"}); await browser.close(); server.close();
