// ===== THE CHARACTER SHEET (Tab): an ashen, ember-lit paper doll. The hero stands in the middle — the hall's own model, with
// whatever is in their hand, turning slowly on a stone pedestal — and the five gear slots hang either side as plaques that
// show the piece, what it gives, and the forge: the upgrades you can buy into it right there (+1 / +5 / +25 per stat, gold per
// upgrade, points used of the item's allowance). Under the hero: health, mana, attack and defense; then the seven fighting
// numbers as medallions. Below: the INVENTORY (the locker as a grid — click a piece to read it, equip it or sell it) and the
// item card. The HUD keeps only what you watch mid-fight and gets bigger.
(function(){
const css=`#hud .bars{width:300px}#hud .bar{height:20px}#hud .bar b{font-size:13px;top:1px}#hud .res{font-size:21px;gap:16px;margin-top:2px}#hud .res .gold{font-size:19px}#hud #xpline{font-size:14px;margin-top:4px}
#hud #gear,#hud #herostats{display:none!important}
#dollbtn{position:absolute;right:110px;top:62px;pointer-events:auto;background:#1c1424;border:2px solid #6b5a3c;border-radius:8px;color:#fff;font-size:16px;padding:5px 10px;cursor:pointer}
#doll{z-index:11;--ash:#0c0b0e;--panel:#141117;--copper:#a5602a;--gold2:#e6b662;--ember:#ff6f22;--cream:#eadfc6;--mute:#9a8e7a;--line:#3a2618;background:#070609e8}
#doll .dl-box{position:relative;width:min(1260px,calc(100vw - 24px));max-height:calc(100vh - 24px);overflow:auto;background:radial-gradient(ellipse at 50% 0%,#1b1519 0,#0c0b0e 55%);border:2px solid var(--copper);border-radius:4px;box-shadow:0 0 0 5px var(--ash),0 0 0 6px var(--line),0 0 50px #000,inset 0 0 70px #000;padding:12px 18px 14px;text-align:left;color:var(--cream);font-family:Georgia,'Times New Roman',serif}
#doll .cn{position:absolute;width:12px;height:12px;background:var(--copper);transform:rotate(45deg);box-shadow:0 0 12px var(--ember),0 0 2px var(--ember);pointer-events:none}#doll .cn.tl{left:6px;top:6px}#doll .cn.tr{right:6px;top:6px}#doll .cn.bl{left:6px;bottom:6px}#doll .cn.br{right:6px;bottom:6px}
#doll h1{display:flex;align-items:center;justify-content:center;gap:14px;font-size:23px;font-weight:normal;letter-spacing:7px;margin:0 0 8px;color:var(--gold2);text-shadow:0 0 14px #ff6f2266,0 2px 0 #000;text-transform:uppercase}#doll h1 .orn{display:block;width:120px;height:1px;background:linear-gradient(90deg,transparent,var(--copper));position:relative}#doll h1 .orn.r{background:linear-gradient(90deg,var(--copper),transparent)}#doll h1 .orn::after{content:'';position:absolute;right:-4px;top:-4px;width:8px;height:8px;background:var(--ember);transform:rotate(45deg);box-shadow:0 0 8px var(--ember)}#doll h1 .orn.r::after{right:auto;left:-4px}
#doll .dl-x{position:absolute;right:12px;top:10px;background:#100d12;border:1px solid var(--copper);border-radius:3px;color:var(--gold2);font-size:18px;width:38px;height:38px;cursor:pointer;z-index:2}
#doll .pl{display:flex;align-items:center;gap:8px;font-size:11px;letter-spacing:3px;color:var(--gold2);text-transform:uppercase;margin:0 0 6px;white-space:nowrap}#doll .pl::before,#doll .pl::after{content:'';flex:1;height:1px;background:linear-gradient(90deg,transparent,var(--copper))}#doll .pl::after{background:linear-gradient(90deg,var(--copper),transparent)}#doll .pl.lft{justify-content:flex-start}#doll .pl.lft::before{display:none}
#doll .dl-grid{display:grid;grid-template-columns:1fr 350px 1fr;gap:14px;align-items:start}#doll .dl-col{display:flex;flex-direction:column;gap:12px}
#doll .dl-pane{background:linear-gradient(#17131a,#0f0d11);border:1px solid var(--copper);border-radius:3px;box-shadow:0 0 0 3px var(--ash),0 0 0 4px var(--line),inset 0 0 24px #000;padding:8px 10px 9px;font-size:12px;line-height:1.3;position:relative}
#doll .dl-figure{position:relative;height:330px;overflow:hidden;border:1px solid var(--copper);border-radius:3px;box-shadow:0 0 0 3px var(--ash),0 0 0 4px var(--line);background:radial-gradient(ellipse at 50% 70%,#2a1a1c,#0d0b10 70%)}
#doll .dl-figure canvas{position:absolute;inset:0;width:100%;height:100%;display:block}
#doll .dl-figure .arch{position:absolute;left:14%;right:14%;top:5%;bottom:-2%;border:1px solid #a5602a88;border-bottom:0;border-radius:50% 50% 0 0/38% 38% 0 0;pointer-events:none;box-shadow:inset 0 0 40px #000c}#doll .dl-figure .arch::before{content:'';position:absolute;left:8px;right:8px;top:8px;bottom:0;border:1px solid #a5602a44;border-bottom:0;border-radius:50% 50% 0 0/38% 38% 0 0}
#doll .dl-figure .vig{position:absolute;inset:0;pointer-events:none;background:radial-gradient(ellipse at 50% 55%,transparent 45%,#0c0b0e 100%)}
#doll .dl-figure .glow{position:absolute;left:50%;bottom:5%;width:70%;height:18%;transform:translateX(-50%);border-radius:50%;background:radial-gradient(#ff6f2233,transparent 70%);pointer-events:none}
#doll .dl-vitals{display:grid;grid-template-columns:1fr 1fr;gap:6px 14px;margin-top:12px}#doll .vt{display:flex;align-items:center;gap:8px;font-size:12px}#doll .vt .vl{width:74px;letter-spacing:1px;color:var(--cream);white-space:nowrap}#doll .vt .vb{flex:1;height:12px;background:#08070a;border:1px solid var(--line);border-radius:2px;overflow:hidden}#doll .vt .vb i{display:block;height:100%;background:linear-gradient(#e0413a,#7a1a16)}#doll .vt .vb.mp i{background:linear-gradient(#4d84ff,#1b3c9a)}#doll .vt .vn{min-width:70px;text-align:right;font-size:13px;color:var(--cream)}#doll .vt .vn b{color:var(--gold2);font-size:15px}
#doll .dl-strip{display:flex;flex-wrap:wrap;justify-content:space-around;gap:6px 4px;margin:10px 0 12px;padding:8px 6px 6px;border-top:1px solid var(--line);border-bottom:1px solid var(--line)}#doll .dl-stat{flex:1 1 90px;max-width:150px;text-align:center}#doll .dl-stat .md{display:inline-flex;align-items:center;justify-content:center;width:46px;height:46px;border-radius:50%;font-size:22px;background:radial-gradient(#221a20,#0d0b10 70%);border:2px solid var(--c,#a5602a);box-shadow:0 0 12px color-mix(in srgb,var(--c,#a5602a) 45%,transparent),inset 0 0 10px #000}#doll .dl-stat i{font-style:normal;font-size:10px;color:var(--gold2);letter-spacing:2px;display:block;margin-top:4px;text-transform:uppercase}#doll .dl-stat b{font-size:17px;color:var(--cream);line-height:1.15;display:block}#doll .dl-stat.big .md{width:54px;height:54px;font-size:26px}#doll .dl-stat em{display:block;height:4px;margin:4px auto 0;width:70%;background:var(--c,#a5602a);border-radius:2px;opacity:.85}
#doll .dl-slot .sl-head{display:flex;gap:10px;align-items:center;margin-bottom:6px}#doll .ic-box{width:56px;height:56px;flex:none;display:flex;align-items:center;justify-content:center;font-size:30px;line-height:1;border:1px solid var(--copper);border-radius:3px;background:radial-gradient(#2a1c22,#100d12 75%);box-shadow:inset 0 0 14px #000;position:relative;cursor:pointer}#doll .ic-box.r3,#doll .ic-box.r4{box-shadow:inset 0 0 14px #000,0 0 14px #ff6f2266}#doll .ic-box.r4{border-color:var(--ember)}#doll .ic-box .tb{position:absolute;right:3px;bottom:1px;font-size:9px;font-style:normal;color:var(--gold2);letter-spacing:1px}#doll .ic-box.empty{color:#4a3d44;font-size:22px;cursor:default}
#doll .dl-slot .nm{font-weight:bold;font-size:13px;line-height:1.15;cursor:pointer}#doll .dl-slot .st{color:var(--mute);font-size:11px;margin-top:2px}#doll .dl-slot .em{color:#7d7286;font-style:italic;font-size:12px}#doll .dl-slot .tier{display:inline-block;font-size:9px;color:var(--gold2);border:1px solid var(--line);border-radius:2px;padding:0 4px;margin-right:4px;vertical-align:1px;letter-spacing:1px}
#doll .fg-meter{position:relative;height:15px;background:#08070a;border:1px solid var(--line);border-radius:2px;overflow:hidden;margin:4px 0}#doll .fg-meter i{position:absolute;left:0;top:0;bottom:0;background:linear-gradient(90deg,#6a2e10,var(--ember))}#doll .fg-meter span{position:absolute;inset:0;text-align:center;font-size:10px;line-height:15px;color:#fff;text-shadow:0 1px 0 #000;letter-spacing:1px}
#doll .fg-cost{font-size:11px;color:var(--mute);margin-bottom:4px}#doll .fg-cost b{color:var(--gold2)}#doll .fg-cost .no{color:#ff9a7a}
#doll .fg-row{display:grid;grid-template-columns:1fr auto auto;gap:5px;align-items:center;background:#08070a99;border:1px solid var(--line);border-radius:2px;padding:4px 7px;margin-top:4px}#doll .fg-row .fg-l{font-size:12px;line-height:1.15}#doll .fg-row .fg-l small{display:block;color:#7d7286;font-size:10px}#doll .fg-row .fg-v{font-size:14px;color:var(--cream);min-width:44px;text-align:right}#doll .fg-row.on .fg-v{color:var(--gold2)}
#doll button{font-family:Georgia,serif}#doll .fg-btns{white-space:nowrap}#doll .fg-btns button{background:linear-gradient(#3a2216,#1d120c);border:1px solid var(--copper);border-radius:2px;color:var(--gold2);font:bold 11px Georgia,serif;padding:4px 6px;margin-left:2px;cursor:pointer}#doll .fg-btns button:hover:not(:disabled){border-color:var(--ember);color:#fff}#doll .fg-btns button:disabled{opacity:.3;cursor:default}
#doll .dl-mini{background:#100d12;border:1px solid var(--copper);border-radius:2px;color:var(--gold2);font:11px Georgia,serif;letter-spacing:1px;padding:5px 9px;cursor:pointer;margin-top:6px}#doll .dl-mini:hover{border-color:var(--ember);color:#fff}
#doll .dl-sets{font-size:12px;color:var(--mute);line-height:1.45}#doll .dl-sets b{color:var(--cream)}#doll .dl-sets .on b{color:var(--gold2)}#doll .dl-sets .sr{display:flex;gap:6px;align-items:baseline;padding:3px 0;border-bottom:1px solid #1d1418}#doll .dl-sets .sr:last-child{border:0}#doll .dl-sets .sc{margin-left:auto;font-size:11px;white-space:nowrap}
#doll .dl-lower{display:grid;grid-template-columns:1fr 1fr;gap:14px;margin-top:12px}
#doll .inv{display:grid;grid-template-columns:repeat(8,1fr);gap:5px}#doll .inv-c{aspect-ratio:1;border:1px solid var(--line);border-radius:2px;background:#0a090c;box-shadow:inset 0 0 10px #000;position:relative;display:flex;align-items:center;justify-content:center;font-size:22px}#doll .inv-c.has{cursor:pointer;border-color:var(--copper);background:radial-gradient(#221820,#0d0b10 75%)}#doll .inv-c.has:hover,#doll .inv-c.sel{border-color:var(--ember);box-shadow:inset 0 0 10px #000,0 0 10px #ff6f2255}#doll .inv-c em{position:absolute;right:3px;bottom:1px;font-size:9px;font-style:normal;color:var(--gold2)}#doll .inv-c.r0{opacity:.75}#doll .inv-c.r1{border-color:#5a8a4a}#doll .inv-c.r2{border-color:#4d84ff}#doll .inv-c.r3{border-color:#b26fe0}#doll .inv-c.r4{border-color:var(--ember)}
#doll .inv-head{display:flex;align-items:center;gap:10px}#doll .inv-head .pl{flex:1;margin:0}#doll .inv-head .dl-mini{margin:0}
#doll .dl-card{min-height:190px;display:flex;flex-direction:column;gap:6px}#doll .cd-head{display:flex;gap:12px;align-items:center}#doll .cd-nm{font-size:16px;font-weight:bold;letter-spacing:1px}#doll .cd-sub{font-size:10px;letter-spacing:2px;color:var(--ember);text-transform:uppercase;margin-top:2px}#doll .cd-stats{border-top:1px solid var(--line);padding-top:6px}#doll .cd-l{font-size:13px;line-height:1.55;color:var(--cream)}#doll .cd-l.mute{color:var(--mute);font-size:11px}#doll .cd-fl{font-style:italic;color:var(--mute);font-size:12px;border-top:1px solid var(--line);padding-top:6px;margin-top:auto}#doll .cd-btns{display:flex;gap:6px;margin-top:6px;flex-wrap:wrap}#doll .cd-btns button{background:linear-gradient(#3a2216,#1d120c);border:1px solid var(--copper);border-radius:2px;color:var(--gold2);font:bold 12px Georgia,serif;letter-spacing:2px;padding:7px 12px;cursor:pointer}#doll .cd-btns button:hover{border-color:var(--ember);color:#fff}#doll .cd-btns button.sell{color:#ff9a7a}
#doll .dl-sum{font-size:12px;color:var(--mute);line-height:1.6}#doll .dl-sum b{color:var(--cream)}#doll .dl-sum .lv{font-size:15px;color:var(--gold2);letter-spacing:2px}#doll .dl-sum .pts{color:var(--gold2)}
#doll .dl-foot{display:flex;justify-content:space-between;align-items:center;gap:10px;margin-top:12px;font-size:11px;color:var(--mute);flex-wrap:wrap;letter-spacing:1px}#doll .dl-foot b{color:var(--gold2)}
#doll .dl-btn{background:linear-gradient(#3a2216,#1d120c);border:1px solid var(--copper);border-radius:2px;color:var(--gold2);font:bold 13px Georgia,serif;letter-spacing:3px;padding:9px 18px;cursor:pointer;box-shadow:0 0 0 2px var(--ash),0 0 0 3px var(--line)}#doll .dl-btn:hover{border-color:var(--ember);color:#fff}
#doll .fg-hint{font-size:12px;color:var(--gold2);text-align:center;padding:6px;border:1px dashed var(--copper);border-radius:3px;margin-bottom:8px;letter-spacing:1px}
@media (max-width:980px){#doll .dl-grid{grid-template-columns:1fr 1fr}#doll .dl-col.mid{grid-column:1/3;order:-1}#doll .dl-figure{height:260px}#doll .dl-lower{grid-template-columns:1fr}}
@media (max-width:640px){#doll .dl-grid{grid-template-columns:1fr}#doll .dl-col.mid{grid-column:auto}#doll h1{font-size:17px;letter-spacing:4px}#doll h1 .orn{width:40px}#doll .inv{grid-template-columns:repeat(6,1fr)}}
@media (max-width:700px){#hud .bars{width:190px}#hud .bar{height:16px}#hud .res{font-size:15px;gap:10px}#hud .res .gold{font-size:14px}#hud #xpline{font-size:12px}}`;
const st=document.createElement('style'); st.textContent=css; document.head.appendChild(st);
const D={open:false,timer:0,html:'',hint:false,sel:null,cv:null};
const SNAME={weapon:'weapon',armor:'armor',charm:'charm',amulet:'amulet',familiar:'familiar'};
const FLAVOR=['Plain, honest work. It will do until something better falls.','Made by a careful hand, and it shows.','Worked with a touch of the old craft; the runes still hold.','Etched with lines that glow faintly in the dark of the hall.','Forged in the founding fire. Things like this are not made any more.'];
const MDC={dps:'#e0413a',dmg:'#ff8a2a',aps:'#e6b662',magic:'#b26fe0',armor:'#5f8fd8',tow:'#5aa85a',hp:'#e06a8a'};
function tierN(it){ return it.tier||tierOf(it.lvl); }
function icon(it,s,extra){ if(!it) return '<div class="ic-box empty">'+SICON[s]+'</div>'; return '<div class="ic-box r'+it.rarity+'" '+(extra||'')+'>'+SICON[it.slot]+'<em class="tb">T'+tierN(it)+'</em></div>'; }
function slotPanel(s){ const it=gear[s], F=Meta.forge; let inner;
  if(!it) inner='<div class="sl-head">'+icon(null,s)+'<div><div class="em">no '+SNAME[s]+' yet</div><button class="dl-mini" data-act="bag" data-slot="'+s+'">PICK ONE FROM THE INVENTORY</button></div></div>';
  else { const pick='data-act="pickeq" data-slot="'+s+'"'; const head='<div class="sl-head">'+icon(it,s,pick)+'<div><div class="nm" '+pick+' style="color:'+RCSS[it.rarity]+'"><span class="tier">T'+tierN(it)+'</span><span class="tier">'+RNAME[it.rarity].toUpperCase()+'</span>'+it.name+'</div><div class="st">'+statStr(it)+'</div></div></div>';
    if(!F) inner=head; else { const gold=Meta.gold(), left=F.left(it), cost=F.cost(it);
      const rows=F.keys(it).map(k=>{ const c=F.can(it,k), v=it.stats[k]||0, pts=(it.ups&&it.ups[k])|0; const btn=n=>'<button data-act="up" data-slot="'+s+'" data-key="'+k+'" data-n="'+n+'"'+(c.ok?'':' disabled title="'+c.why+'"')+'>+'+n+'</button>';
        return '<div class="fg-row'+(pts?' on':'')+'"><span class="fg-l">'+F.label(k)+'<small>+'+F.inc[k]+(k==='fproj'?'':/dmg|hp|regen/.test(k)?'':'%')+' per upgrade'+(pts?' · '+pts+' put in':'')+(c.ok||!left?'':' · '+c.why)+'</small></span><span class="fg-v">'+(v?F.fmt(k,v):'—')+'</span><span class="fg-btns">'+btn(1)+btn(5)+btn(25)+'</span></div>'; }).join('');
      inner=head+'<div class="fg-meter"><i style="width:'+Math.round(100*F.used(it)/F.max(it))+'%"></i><span>'+F.used(it)+' / '+F.max(it)+' upgrades'+(left?'':' · MAXED')+'</span></div>'+
        (left?'<div class="fg-cost">next upgrade <b>'+Meta.fmtG(cost)+' ●</b>'+(gold<cost?' · <span class="no">need '+Meta.fmtG(cost-gold)+' more</span>':'')+'</div>':'')+rows; } }
  return '<div class="dl-slot dl-pane '+s+'" data-slot="'+s+'"><div class="pl">'+SNAME[s]+'</div>'+inner+'</div>'; }
function medal(k,l,v,big){ return '<div class="dl-stat'+(big?' big':'')+'" style="--c:'+(MDC[k]||'#a5602a')+'"><span class="md">'+l.split(' ')[0]+'</span><i>'+l.split(' ').slice(1).join(' ')+'</i><b>'+v+'</b><em></em></div>'; }
function setsPanel(){ if(!Meta.sets) return ''; const c=Meta.sets.counts(), a=Meta.sets.active(); const rows=Object.keys(Meta.sets.SETS).map(n=>{ const S=Meta.sets.SETS[n], k=c[n]|0, act=a.find(x=>x.name===n); return '<div class="sr'+(act?' on':'')+'"><span>'+S.ic+' <b>'+n+' '+k+'/5</b>'+(act?'<br>'+act.text:k?'<br><span style="color:#6f6459">'+(k>=3?'':'3 pieces: '+S.text[0]+' · ')+'5 pieces: '+S.text[1]+'</span>':'<br><span style="color:#6f6459">3 pieces: '+S.text[0]+'</span>')+'</span></div>'; }).join('');
  return '<div class="dl-pane dl-sets"><div class="pl">set bonuses</div>'+rows+'<div style="font-size:10px;color:#6f6459;margin-top:4px">pieces named "of the …" count toward a set</div></div>'; }
function bagGrid(){ const bag=Meta.bag(), cap=Meta.BAG_CAP||24; let cells=''; for(let i=0;i<cap;i++){ const it=bag[i]; if(!it){ cells+='<div class="inv-c"></div>'; continue; } const sel=D.sel&&D.sel.from==='bag'&&D.sel.id===it.id; cells+='<div class="inv-c has r'+it.rarity+(sel?' sel':'')+'" data-act="pick" data-id="'+it.id+'" title="'+it.name+'"><span>'+SICON[it.slot]+'</span><em>T'+tierN(it)+'</em></div>'; } return cells; }
function selected(){ if(!D.sel) return null; if(D.sel.from==='eq'){ const it=gear[D.sel.slot]; return it&&it.id===D.sel.id?it:null; } return Meta.bag().find(b=>b.id===D.sel.id)||null; }
function summaryCard(){ const M=Meta, pts=M.points?M.points():0, lvl=M.level?M.level():1, xp=M.xp?M.xp():0, next=M.xpToNext?M.xpToNext(lvl):100;
  const skills=(M.SKILLS||[]).filter(s=>M.skill(s.id)>0).map(s=>'<b>'+s.name+'</b> '+M.skill(s.id)+' ('+M.skillValue(s.id)+')').join(' · ')||'none yet';
  return '<div class="dl-card dl-sum"><div class="lv">LEVEL '+lvl+'</div><div>'+xp.toLocaleString('en-US')+' / '+next.toLocaleString('en-US')+' XP · <b>● '+M.fmtG(M.gold())+' gold</b> to forge with'+(pts?' · <span class="pts">✦ '+pts+' skill point'+(pts===1?'':'s')+' waiting at the trainer</span>':'')+'</div><div>Trainer skills (one point per level): '+skills+'</div><div class="cd-fl">Click a piece in the inventory, or one you wear, to read it here. Gold buys upgrades that stay on the item; skill points are spent at the trainer.</div></div>'; }
function itemCard(){ const it=selected(); if(!it){ D.sel=null; return summaryCard(); } const F=Meta.forge, eq=D.sel.from==='eq', lines=statStr(it).split(' · ').map(s=>'<div class="cd-l">'+s+'</div>').join('');
  return '<div class="dl-card"><div class="cd-head">'+icon(it,it.slot)+'<div><div class="cd-nm" style="color:'+RCSS[it.rarity]+'">'+it.name+'</div><div class="cd-sub">'+RNAME[it.rarity]+' '+it.slot+' · tier '+tierN(it)+(eq?' · worn':'')+'</div></div></div><div class="cd-stats">'+lines+(F?'<div class="cd-l mute">forge allowance: '+F.used(it)+' / '+F.max(it)+' upgrades'+(eq?' — buy them in its slot above':'')+'</div>':'')+'</div><div class="cd-fl">'+FLAVOR[Math.min(4,it.rarity|0)]+'</div><div class="cd-btns">'+(eq?'<button data-act="unequip" data-slot="'+it.slot+'">UNEQUIP</button>':'<button data-act="equip" data-id="'+it.id+'">EQUIP</button><button class="sell" data-act="sell" data-id="'+it.id+'">SELL · '+Meta.fmtG(it.value||0)+' ●</button>')+'</div></div>'; }
function heroName(){ try{ const h=window.__heroes&&window.__heroes.list().find(h=>h.id===window.__heroes.pick()); return h?h.name:'GNOME WARDEN'; }catch(e){ return 'GNOME WARDEN'; } }
function build(){ const stt=window.__feel?window.__feel.stats():{dmg:heroDmg(),aps:+(1/swingDur()).toFixed(2),magic:0,dps:Math.round(heroDmg()/swingDur()),armor:heroStat('def'),tow:Math.round(((1+heroStat('tow')/100)*heroMult('tow')-1)*100),hp:Math.round(hero.hp)+'/'+hero.max};
  const M=Meta, lvl=M.level?M.level():1, bagN=Meta.bag().length, cap=Meta.BAG_CAP||24, hpF=Math.max(0,Math.min(1,hero.hp/hero.max)), mnF=Math.max(0,Math.min(1,S.mana/Math.max(500,S.mana)));
  return '<div class="dl-box"><i class="cn tl"></i><i class="cn tr"></i><i class="cn bl"></i><i class="cn br"></i><button class="dl-x" data-act="close">✕</button><h1><span class="orn"></span>The '+heroName()+'<span class="orn r"></span></h1>'+
    (D.hint&&Meta.forge?'<div class="fg-hint">🔨 The anvil: buy upgrades for it with gold, straight into each piece of gear below (up to its allowance)</div>':'')+
    '<div class="dl-grid"><div class="dl-col">'+slotPanel('weapon')+slotPanel('armor')+slotPanel('familiar')+'</div>'+
    '<div class="dl-col mid"><div class="dl-figure"><div class="arch"></div><div class="glow"></div><div class="vig"></div></div>'+
      '<div class="dl-vitals"><div class="vt"><span class="vl">❤ HP</span><div class="vb"><i style="width:'+Math.round(hpF*100)+'%"></i></div><span class="vn">'+stt.hp+'</span></div><div class="vt"><span class="vl">⚔ ATTACK</span><span class="vn"><b>'+stt.dmg+'</b></span></div><div class="vt"><span class="vl">💧 MANA</span><div class="vb mp"><i style="width:'+Math.round(mnF*100)+'%"></i></div><span class="vn">'+Math.round(S.mana)+'</span></div><div class="vt"><span class="vl">🛡 DEFENSE</span><span class="vn"><b>'+stt.armor+'%</b></span></div></div></div>'+
    '<div class="dl-col">'+slotPanel('charm')+slotPanel('amulet')+setsPanel()+'</div></div>'+
    '<div class="dl-strip">'+medal('dps','💥 DPS',stt.dps,true)+medal('dmg','⚔ Damage',stt.dmg)+medal('aps','⚡ Swings / s',(+stt.aps).toFixed(2))+medal('magic','✨ Magic',stt.magic||'—')+medal('armor','🛡 Armor',stt.armor+'%')+medal('tow','🏹 Defenses',(stt.tow>=0?'+':'')+stt.tow+'%')+medal('hp','❤ Health',stt.hp)+'</div>'+
    '<div class="dl-lower"><div class="dl-pane"><div class="inv-head"><div class="pl lft">inventory · '+bagN+' / '+cap+'</div><button class="dl-mini" data-act="selljunk">SELL JUNK</button><button class="dl-mini" data-act="tavern">🍺 TAVERN</button></div><div class="inv" style="margin-top:8px">'+bagGrid()+'</div></div><div class="dl-pane">'+itemCard()+'</div></div>'+
    '<div class="dl-foot"><span>LEVEL '+lvl+' · <b>● '+M.fmtG(M.gold())+' gold</b></span><span><b>Tab</b> closes · click a piece to read it · gold buys upgrades that stay on the item</span></div></div>'; }
let el=null;
function ensure(){ if(el) return; el=document.createElement('div'); el.id='doll'; el.className='screen hide'; document.body.appendChild(el); D.cv=document.createElement('canvas');
  el.addEventListener('click',e=>{ const a=e.target.closest('[data-act]'); if(!a){ if(e.target===el) close(); return; } const act=a.dataset.act, ds=a.dataset;
    if(act==='close') close(); else if(act==='tavern'){ close(); Meta.open(); if(typeof Tavern!=='undefined'&&Tavern.tab) Tavern.tab('shop'); }
    else if(act==='bag'){ const s=ds.slot, cand=Meta.bag().filter(b=>b.slot===s); if(cand.length){ D.sel={id:cand[0].id,from:'bag'}; render(); const inv=el.querySelector('.inv'); if(inv&&inv.scrollIntoView) inv.scrollIntoView({block:'nearest',behavior:'smooth'}); } else { toast('Nothing for that slot in the inventory — the tavern sells some'); close(); Meta.open(); if(typeof Tavern!=='undefined'&&Tavern.tab) Tavern.tab('shop'); } }
    else if(act==='pick'){ D.sel=D.sel&&D.sel.from==='bag'&&D.sel.id===ds.id?null:{id:ds.id,from:'bag'}; render(); }
    else if(act==='pickeq'){ const it=gear[ds.slot]; D.sel=it?(D.sel&&D.sel.from==='eq'&&D.sel.id===it.id?null:{id:it.id,from:'eq',slot:ds.slot}):null; render(); }
    else if(act==='equip'){ const it=Meta.bag().find(b=>b.id===ds.id); if(it&&Meta.equip(ds.id)){ toast('Equipped '+it.name); D.sel={id:it.id,from:'eq',slot:it.slot}; } render(); }
    else if(act==='unequip'){ if(Meta.unequip(ds.slot)) toast('Unequipped'); D.sel=null; render(); }
    else if(act==='sell'){ const it=Meta.bag().find(b=>b.id===ds.id); const g=Meta.sell(ds.id); if(g) toast('Sold '+(it?it.name:'item')+' for '+Meta.fmtG(g)+' ●'); D.sel=null; render(); }
    else if(act==='selljunk'){ const r=Meta.sellJunk(); toast(r.n?'Sold '+r.n+' item'+(r.n===1?'':'s')+' for '+Meta.fmtG(r.gold)+' ●':'Nothing worth selling'); D.sel=null; render(); }
    else if(act==='up'){ const it=gear[ds.slot]; if(!it||!Meta.forge) return; const n=Meta.forge.upgrade(it.id,ds.key,+ds.n||1); if(!n){ const c=Meta.forge.can(it,ds.key); toast(c.why||'cannot upgrade that'); } render(); } });
  const b=document.createElement('button'); b.id='dollbtn'; b.title='Character sheet & forge (Tab)'; b.textContent='👤'; b.addEventListener('click',open); $('hud').appendChild(b); }
function render(){ const h=build(); if(h!==D.html){ D.html=h; el.innerHTML=h; } const f=el.querySelector('.dl-figure'); if(f&&D.cv.parentNode!==f) f.prepend(D.cv); }
function open(hint){ ensure(); if(D.open||S.phase==='start'||(typeof Tavern!=='undefined'&&Tavern&&Tavern.isOpen())) return false; D.open=true; D.hint=hint===true; D.sel=null; render(); el.classList.remove('hide'); if(document.exitPointerLock) document.exitPointerLock(); document.body.classList.remove('play'); for(const k in K) K[k]=0; D.timer=setInterval(render,250); return true; }
function close(){ if(!D.open) return false; D.open=false; el.classList.add('hide'); clearInterval(D.timer); D.timer=0; return true; }
function toggle(){ return D.open?close():open(); }
{ const prev=Meta.isOpen; Meta.isOpen=()=>D.open||!!prev(); }
// ----- the portrait: the hall's own hero model, lit for a portrait and rendered by a second camera that only sees layer 1
// (the hero, their weapon, the pedestal and these lights), then copied into the sheet's figure window
let PC=null, PL=null;
function portraitSetup(){ if(PC) return; PC=new THREE.PerspectiveCamera(24,1,.1,80); PC.layers.set(1); PL=new THREE.Group();
  const key=new THREE.DirectionalLight(0xffe4bd,1.15); key.position.set(1.8,3,2.6); const rim=new THREE.DirectionalLight(0xff7a2a,1.0); rim.position.set(-1.6,1.6,-2.4); const fill=new THREE.HemisphereLight(0xfff1dc,0x2b1a14,.7);
  [key,rim,fill].forEach(l=>{ l.layers.set(1); PL.add(l); }); PL.add(key.target); PL.add(rim.target);
  const ped=new THREE.Mesh(new THREE.CylinderGeometry(.95,1.08,.16,28),new THREE.MeshToonMaterial({color:0x2a2228})); ped.position.y=-.08; ped.layers.set(1); PL.add(ped);
  const ring=new THREE.Mesh(new THREE.TorusGeometry(.97,.035,8,48),new THREE.MeshToonMaterial({color:0xb06b2a,emissive:0x6a2c08})); ring.rotation.x=PI/2; ring.position.y=-.005; ring.layers.set(1); PL.add(ring); scene.add(PL); }
function dollFrame(dt){ const fig=el&&el.querySelector('.dl-figure'); if(!fig||!D.cv) return; portraitSetup(); const cv=D.cv, r=fig.getBoundingClientRect(); if(r.width<8||r.height<8) return; const pr=Math.min(2,devicePixelRatio||1); const w=Math.round(r.width*pr), h=Math.round(r.height*pr); if(cv.width!==w||cv.height!==h){ cv.width=w; cv.height=h; }
  const glb=useGLB&&GLBH; const root=glb?GLBH.wrap:H.g; root.traverse(o=>o.layers.enable(1)); if(glb){ playHero('idle',{fade:.2}); GLBH.mixer.update(dt); } if(window.__weapons&&window.__weapons.tick) window.__weapons.tick(dt);   // the sheet pauses the hall, not the hero's breathing or the lash
  PL.position.set(hero.x,hero.y,hero.z); const t=performance.now()/1000, ya=hero.yaw+Math.sin(t*.4)*.45, hh=(glb&&GLBH.height?GLBH.height*GLBH.scale:HERO_H)||2.6, cy=hero.y+hh*.5, dist=hh*2.75;   // the hero faces +z at yaw 0 (the hit cone's way); the camera stands in front and drifts side to side, framing the whole figure and the pedestal
  PC.position.set(hero.x+Math.sin(ya)*dist,cy+hh*.12,hero.z+Math.cos(ya)*dist); PC.lookAt(hero.x,cy-hh*.02,hero.z); PC.aspect=w/h; PC.updateProjectionMatrix();
  const W=renderer.domElement.width, Hh=renderer.domElement.height, rw=Math.min(w,W), rh=Math.min(h,Hh), bg=scene.background; scene.background=null;
  renderer.setScissorTest(true); renderer.setViewport(0,0,rw,rh); renderer.setScissor(0,0,rw,rh); renderer.setClearColor(0x0d0b10,1); renderer.clear(); renderer.render(scene,PC); renderer.setScissorTest(false); renderer.setViewport(0,0,W,Hh); renderer.setScissor(0,0,W,Hh); scene.background=bg;
  const g=cv.getContext('2d'); g.drawImage(renderer.domElement,0,Hh-rh,rw,rh,0,0,w,h); }
{ const baseFrame=frame; frame=function(now){ if(!D.open) return baseFrame(now); requestAnimationFrame(frame); const dt=Math.min(.05,(now-lastT)/1000); lastT=now; update(dt); dollFrame(dt); }; }
addEventListener('keydown',e=>{ if(e.code==='Tab'||e.code==='KeyC'){ if(S.phase==='start'||S.phase==='dead'||S.phase==='won') return; if(e.code==='Tab') e.preventDefault(); if(typeof Tavern!=='undefined'&&Tavern&&Tavern.isOpen()) return; toggle(); e.stopImmediatePropagation(); return; }
  if(D.open){ if(e.code==='Escape'||e.code==='KeyI'||e.code==='KeyB'){ e.preventDefault(); close(); } e.stopImmediatePropagation(); } },true);
ensure();
window.__doll={open,close,isOpen:()=>D.open,html:()=>el?el.innerHTML:'',select:(id,from,slot)=>{ D.sel=id?{id,from:from||'bag',slot}:null; if(D.open) render(); },selected:()=>D.sel,portrait:()=>D.cv?{w:D.cv.width,h:D.cv.height,cam:!!PC}:null,forge:()=>null,setForge:()=>{}};
})();
